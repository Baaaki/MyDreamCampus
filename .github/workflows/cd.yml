name: CD

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  workflow_dispatch:

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  # ─────────────────────────────────────────────
  # Detect which services changed (skip unchanged)
  # ─────────────────────────────────────────────
  changes:
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.filter.outputs.changes }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          # shared/ is in every filter on purpose: it is compiled INTO each
          # image, so a change there makes ten images stale at once.
          filters: |
            auth-service:
              - 'new-backend/services/auth-service/**'
              - 'new-backend/shared/**'
            staff-service:
              - 'new-backend/services/staff-service/**'
              - 'new-backend/shared/**'
            student-service:
              - 'new-backend/services/student-service/**'
              - 'new-backend/shared/**'
            catalog-service:
              - 'new-backend/services/catalog-service/**'
              - 'new-backend/shared/**'
            enrollment-service:
              - 'new-backend/services/enrollment-service/**'
              - 'new-backend/shared/**'
            attendance-service:
              - 'new-backend/services/attendance-service/**'
              - 'new-backend/shared/**'
            grades-service:
              - 'new-backend/services/grades-service/**'
              - 'new-backend/shared/**'
            meal-service:
              - 'new-backend/services/meal-service/**'
              - 'new-backend/shared/**'
            payment-service:
              - 'new-backend/services/payment-service/**'
              - 'new-backend/shared/**'
            notification-service:
              - 'new-backend/services/notification-service/**'
              - 'new-backend/shared/**'

  # ─────────────────────────────────────────────
  # Build & push changed services to GHCR
  # ─────────────────────────────────────────────
  build-push:
    needs: changes
    if: needs.changes.outputs.services != '[]' && needs.changes.outputs.services != ''
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJSON(needs.changes.outputs.services) }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/mydreamcampus-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      # Build, scan, then push: scanning after the push only reports on an
      # image that is already in the registry under :latest.
      - name: Build for scanning
        uses: docker/build-push-action@v6
        with:
          # Repo root, matching compose: every service Dockerfile copies both
          # its own directory and new-backend/shared, so a context rooted at
          # the service cannot see its replace target.
          context: .
          file: ./new-backend/services/${{ matrix.service }}/Dockerfile
          load: true
          push: false
          platforms: linux/amd64
          tags: scan/mydreamcampus-${{ matrix.service }}:${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,scope=${{ matrix.service }},mode=max

      # Pinned to a commit: in March 2026 this action's version tags were
      # force-pushed to malicious commits and later re-created with a v
      # prefix (the 0.28.0 this used no longer resolves). A SHA cannot move.
      - name: Trivy report (CRITICAL, HIGH)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: scan/mydreamcampus-${{ matrix.service }}:${{ github.sha }}
          format: sarif
          output: trivy-${{ matrix.service }}.sarif
          severity: CRITICAL,HIGH
          exit-code: "0"
          ignore-unfixed: true

      - name: Upload Trivy results to GitHub Security
        if: always() && hashFiles(format('trivy-{0}.sarif', matrix.service)) != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-${{ matrix.service }}.sarif
          category: trivy-${{ matrix.service }}

      # HIGH stays a report; a fixable CRITICAL stops the image from being
      # pushed at all.
      - name: Trivy gate (CRITICAL)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: scan/mydreamcampus-${{ matrix.service }}:${{ github.sha }}
          format: table
          severity: CRITICAL
          exit-code: "1"
          ignore-unfixed: true

      - name: Push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./new-backend/services/${{ matrix.service }}/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.service }}
