/// <reference types="nativewind/types" />
// Expo puts this reference in expo-env.d.ts, which it writes on `expo start`
// and which is gitignored. Without it a clean checkout (CI) has no '*.css'
// module declaration for the global.css import in app/_layout.tsx.
/// <reference types="expo/types" />
